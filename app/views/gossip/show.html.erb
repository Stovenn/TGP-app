<!DOCTYPE html>
<html>
<head>
	<title>Gossip <%= @gossip.id%></title>
</head>
<body>
  <div class="container">
    <div class="card ">
	  <div class="card-header bg-primary text-white">
	      <h3><%= @gossip.title %></h3>
	    </div>
	    <div class="card-body">
	      <blockquote class="blockquote mb-0">
	      <p><%= @gossip.content %></p>
	      <footer class="blockquote-footer"><cite title="Source Title"><%= @gossip.user.first_name%> <%= @gossip.user.last_name%></cite> <a href="<%= user_path(@gossip.user.id)%>">(Afficher le profil de l'auteur)</a></footer>
	      </blockquote>
	    </div>
	    <div class="card-footer small">Posté le <%= @gossip.created_at.strftime('%d-%m-%Y') %></div>
    </div>
  </div>
</body>
</html>